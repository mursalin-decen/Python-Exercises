# 9. Can you change the values inside a list which is contained in set S?
# s = {8, 7, 12, "zamzam", [1,2]}
s= {8,7,12,"zamzam", [1,2]}

#No, we cannot change the values inside a list contained in a set
#because a list cannot even exist inside a set. Python will give an error.